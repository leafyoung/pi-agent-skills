Check your LLM AI coding principles and then do a comprehensive search of this application and suggest what we can do to refactor it to better align to those principles. Also point out any code that can be deleted, any files that can be deleted, things that should be renamed, things that should be restructured.

Then do a write-up of what that looks like. Keep it high level so that it's easy to read and not too complex.

Add sections for **High**, **Medium**, and **Lower** priority.

If something doesn't need to be changed, don't change it. Only flag changes that genuinely help align to the LLM AI coding principles — not changes for the sake of changes.

Save the write-up to a markdown file in the project root (e.g. `llm-refactor-plan.md`).
